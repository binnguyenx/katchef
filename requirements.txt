# Web API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Config & validation
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# Gemini — vision (backend/services/vision_service.py uses google.genai)
google-genai>=1.0.0

# Gemini — chat (backend/services/chatbot_service.py; migrate to google-genai when ready)
google-generativeai>=0.8.0

# Firebase Admin (Firestore persistence)
firebase-admin>=6.6.0
